import { Box } from '@mui/material'
import { ScanSerch } from '../components/scan'
import '../components/scan/scanEstilos.css'

export const Scan = () => {
  return (
    <Box className='container' > 
      <ScanSerch />
    </Box>
  )
}
